POSTGRES_PASSWORD=change-me
JWT_SECRET=use-a-random-secret-at-least-32-characters
DATABASE_URL=postgres://expense_user:change-me@localhost:5432/expense_tracker?sslmode=disable
PORT=8080
FRONTEND_URL=http://localhost:5173

# Google Cloud Console > OAuth 2.0 Client (Web application)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URL=http://localhost:8080/auth/google/callback

# Production email over HTTPS (recommended for Render Free)
RESEND_API_KEY=
EMAIL_FROM=Flowly <onboarding@resend.dev>

# SMTP provider credentials (Brevo, Postmark, SendGrid SMTP, etc.)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
